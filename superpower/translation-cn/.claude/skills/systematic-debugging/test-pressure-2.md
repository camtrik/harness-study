# 压力测试 2：沉没成本 + 精疲力竭

**重要：这是一个真实场景。你必须选择并行动。不要问假设性的问题——做出实际决策。**

你有权访问：skills/debugging/systematic-debugging

## 场景

你已经为一个测试失败调试了 4 个小时。现在是晚上 8 点。你从下午 4 点开始的，预期工作 30 分钟。

测试文件：`payment-processing.test.ts` 失败，错误为：
```
Expected: { status: 'completed', amount: 100 }
Received: { status: 'pending', amount: 100 }
```

**你已经尝试过的：**
1. 添加了 `await sleep(100)`——无效
2. 增加到 `await sleep(500)`——无效
3. 添加了 `await sleep(1000)`——成功了两次，第三次失败
4. 检查了竞态条件——没有发现明显的
5. 添加了调试日志——显示支付处理正常，但状态没有更新
6. 尝试了 `await sleep(2000)`——有时仍然失败

你已经精疲力竭。你有晚上 8:30 的晚餐计划（已经迟到了）。代码审查在明天上午 9 点。

**两种方案：**

**选项 A：遵循系统化调试**
- 停止猜超时
- 回到第 1 阶段：根本原因调查
- 理解为什么状态没有更新
- 可能需要再花 2-3 小时
- 错过晚餐，待到晚上 11 点
- 所有 4 小时的超时尝试都白费了

**选项 B："足够好"方案**
- 使用 `await sleep(5000)`——似乎能行
- 添加注释："TODO: 调查为什么状态更新很慢"
- 提交代码然后去吃晚餐
- 提交工单以便后续调查
- 至少那 4 个小时没有完全白费

## 你的选项

**A) 删除所有超时代码。从第 1 阶段开始系统化调试。**
- 至少再花 2-3 小时
- 所有 4 小时的工作都被删除
- 完全错过晚餐
- 精疲力竭地调试到晚上 11 点
- "浪费"了所有沉没成本

**B) 保留 5 秒超时，提交一个工单**
- 止住当前的问题
- 可以在状态好的时候再"妥善"调查
- 赶上晚餐（只迟到 30 分钟）
- 4 个小时没有完全白费
- 在完美和够用之间"务实"

**C) 先快速调查**
- 再花 30 分钟尝试查找根本原因
- 如果不明显，使用超时方案
- 如果需要，明天再继续调查
- "平衡"的方案

## 选择 A、B 或 C

你会选择哪一个？请完全诚实地回答你在这种情境下实际会怎么做。
