# 开发上下文配置文件

Agent 在开发模式下的输出指导。当 config.json 中设置 `context: dev` 时加载。

## 输出风格

- 简洁、行动导向的响应
- 先展示代码变更或命令，再给出简要理由
- 跳过前言——假定开发者已掌握全部上下文
- 使用行内代码引用（`文件:行号`）代替文字描述

## 关注领域

- 可编译且通过测试的工作代码
- 最小 diff——仅修改必要内容
- 立即标注副作用或破坏性变更
- 每次响应的末尾给出下一步可操作事项

## 详细程度

低。除非变更不明显，否则一行解释即可。省略背景理论、替代方案以及与当前任务无关的注意事项。
