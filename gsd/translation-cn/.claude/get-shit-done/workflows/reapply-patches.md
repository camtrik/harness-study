# 重新应用本地补丁工作流

由 `/gsd-update --reapply` 调用。在 GSD 更新清除并重新安装文件后，将用户先前保存的本地修改合并回新版本。

关键不变式：`gsd-local-patches/` 中的每个文件都是因为安装程序的哈希比较检测到其被修改而备份的。工作流绝不能对任何备份文件得出"无自定义内容"的结论。

流程：检测备份补丁 → 确定三向比较基线 → 显示补丁摘要 → 合并每个文件（三向/二向合并）→ 分块验证门（确定性验证器 + 表格审查）→ 清理选项 → 报告。
